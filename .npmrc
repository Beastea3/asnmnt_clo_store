# Create React App + ESLint resolve the same plugins via two paths under pnpm’s
# symlinked layout, which triggers: Plugin "react" was conflicted between …
# Hoisting eslint-related packages restores a single plugin instance.
public-hoist-pattern[]=*eslint*
